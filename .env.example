# Server Configuration
PORT=5001
NODE_ENV=development

# Frontend URL for CORS configuration
FRONTEND_URL=http://localhost:3000

# Production Database Connection (automatic SSH tunnels)
PROD_DB_HOST=localhost
PROD_DB_PORT=5433
PROD_DB_NAME=misfits
PROD_DB_USER=dev
PROD_DB_PASSWORD=postgres

# SSH Tunnel Configuration (OPTIONAL - only if no existing tunnel)
# Leave SSH_KEY_PATH empty to use existing tunnel on port 5433
# To create tunnel manually: ssh -f -i ~/Downloads/claude-control-key -N -L 5433:misfits.cgncbvolnhe7.ap-south-1.rds.amazonaws.com:5432 claude-control@15.207.255.212
SSH_KEY_PATH=
SSH_HOST=15.207.255.212
SSH_USER=claude-control
DB_HOST=misfits.cgncbvolnhe7.ap-south-1.rds.amazonaws.com
DB_PORT=5432

# Local Database Configuration (for operations data)
# Update LOCAL_DB_USER based on your system's PostgreSQL user (commonly 'postgres', 'rental', or your username)
LOCAL_DB_HOST=localhost
LOCAL_DB_PORT=5432
LOCAL_DB_NAME=misfits_ops
LOCAL_DB_USER=postgres
LOCAL_DB_PASSWORD=

# Misfits Backend (for support ticket proxy)
MISFITS_BACKEND_URL=http://localhost:8000
SUPPORT_ADMIN_PHONE=919045162855

# Start Your Club upstream write API (required for create/update actions like Add Lead)
MISFITS_API_URL=https://prod.misfits.net.in/api/v1
MISFITS_API_TOKEN=your-misfits-api-token

# gRPC Configuration (required for StartYourClub and VenueRepository integrations)
# Keep GRPC_API_KEY secret and never commit real values.
GRPC_API_KEY=your-grpc-api-key
# Optional override; current production endpoint shown as default.
GRPC_HOST=15.207.255.212:8001
# Optional path override for grpcurl binary
# GRPCURL_BIN=/absolute/path/to/grpcurl

# Business Logic Configuration
TARGET_MEETUP_INCREASE=50
MIN_TARGET_MEETUPS=300

# Redis Configuration (optional)
REDIS_URL=redis://localhost:6379

# JWT Secret (for authentication features)
JWT_SECRET=your-jwt-secret-here
